/*
Documentation:
0. preprocessing
a. revise rotation algorithm, make it work in 2s frames;
b. revise calibration part...
c. cut the processed image into edge, (save it, optional);

1. revise the identification part;
a. resize the image to imagesize*1.2;
b. cut image into 5 pieces and detect them;
c. process results with Rect parameters, coordinate/1.2, and return results;

2. tet performance.

*/
